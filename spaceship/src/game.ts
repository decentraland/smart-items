// import Spaceship from './item'

// const spaceship = new Spaceship()
// spaceship.init()

// const host = new Entity()
// host.addComponent(
//   new Transform({
//     position: new Vector3(16, 0, 16)
//   })
// )
// engine.addEntity(host)

// spaceship.spawn(host, {})
