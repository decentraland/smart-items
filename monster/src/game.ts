// import Button from "./item";

// const button = new Button();
// button.init();
// const host = new Entity();
// host.addComponent(
//   new Transform({
//     position: new Vector3(8, 0, 8)
//   })
// );
// engine.addEntity(host);
// button.spawn(host, { animation: "run" });
